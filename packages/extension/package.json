{
  "name": "@technical-clipper/extension",
  "version": "0.0.0",
  "private": true,
  "description": "Thin Chromium Manifest V3 extension shell. Scaffold only in Phase 0 — capture action, preview, and Obsidian handoff land in Phase 9.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.278"
  }
}
