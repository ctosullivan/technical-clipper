/**
 * Manifest V3 service worker entrypoint.
 *
 * Phase 0 scaffold only — no capture action, preview, or Obsidian handoff
 * exists yet. See `planning/ROADMAP.md` (Phase 9) once the Phase 1 plans are
 * written.
 */
export const EXTENSION_PACKAGE_STATUS = 'scaffold' as const;
