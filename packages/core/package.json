{
  "name": "@technical-clipper/core",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-independent core: typed IR contracts, provenance/confidence semantics, normalization, and hashing. Scaffold only in Phase 0 — real behaviour lands in Phase 3.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  }
}
