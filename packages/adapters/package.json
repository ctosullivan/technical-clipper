{
  "name": "@technical-clipper/adapters",
  "version": "0.0.0",
  "private": true,
  "description": "The ChatGPT current-branch conversation adapter and the ClipSpec override seam. Docusaurus tab groups are handled by a component detector in @technical-clipper/detectors.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@technical-clipper/core": "workspace:*",
    "@technical-clipper/detectors": "workspace:*"
  },
  "devDependencies": {
    "linkedom": "^0.18.0"
  }
}
