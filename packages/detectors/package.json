{
  "name": "@technical-clipper/detectors",
  "version": "0.0.0",
  "private": true,
  "description": "Component detectors that identify code/terminal/tab-group structures in a cloned rendered DOM for sentinel-based pre-extraction. Scaffold only in Phase 0 — real behaviour lands in Phase 5.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@technical-clipper/core": "workspace:*"
  }
}
