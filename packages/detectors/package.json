{
  "name": "@technical-clipper/detectors",
  "version": "0.0.0",
  "private": true,
  "description": "Component detectors (code/pre-code, block-level code, Prism, Highlight.js, terminal sessions, virtualized-editor guard) that identify code structures in a cloned rendered DOM for sentinel-based pre-extraction.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@technical-clipper/core": "workspace:*"
  },
  "devDependencies": {
    "linkedom": "^0.18.0"
  }
}
